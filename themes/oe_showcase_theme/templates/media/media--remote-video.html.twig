{#
/**
 * @file
 * Theme override to display remote video media with optional copyright overlay.
 */
#}
{% set copyright_value = media.hasField('field_media_copyright') ? media.field_media_copyright.value : '' %}
{% set media_output = content.oe_media_oembed_video %}

<div{{ attributes }}>
  {{ title_suffix.contextual_links }}
  {{ content|without('oe_media_oembed_video', 'field_media_copyright') }}
  {% if media_output is not empty %}
    {% if copyright_value %}
      {{ pattern('copyright_wrapper', {
        content: media_output,
        copyright: {
          content: copyright_value,
        },
      }) }}
    {% else %}
      {{ media_output }}
    {% endif %}
  {% endif %}
</div>
