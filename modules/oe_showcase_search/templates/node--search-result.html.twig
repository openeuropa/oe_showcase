{#
/**
 * @file
 * Search result template.
 */
#}

{{ pattern('card', {
  variant: 'search',
  title: label,
  text: content.body,
  image: content.field_image[0],
  content: content.field_category
}) }}
